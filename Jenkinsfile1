pipeline {
    agent {
       label "master"
    }
    stages {
          stage("connect") {
             steps {
               script {
                   sh '''
                   make image push VERSION=test_valek 
                   make image pull VERSION=test_valek
                   '''
                   
               }
             }
          }
        }
}

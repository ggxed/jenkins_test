pipeline {
    agent {
       label "master"
    }
    stages {
       stage("connect") {
          steps {
            script {
                sh "make image push"
            }
          }
       }
    }
}

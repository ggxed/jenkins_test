pipeline {
    agent {
       label "master"
    }
    stages {
          stage("connect") {
             steps {
               script {
                   sh '''
                   make image push VERSION=test
                   make image pull VERSION=test
                   '''
                   
               }
             }
          }
        }
}
